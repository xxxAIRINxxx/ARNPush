source 'https://github.com/CocoaPods/Specs.git'

target 'ARNPush', :exclusive => true do
  pod "ARNPush", :path => "../"
end

target 'Tests', :exclusive => true do
  pod "ARNPush", :path => "../"

  pod 'Specta', '~> 0.2.1'
  pod 'Expecta'
  pod 'FBSnapshotTestCase'
  pod 'Expecta+Snapshots'
end
